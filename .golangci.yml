version: "2"
linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - gosec
    - nilerr
    - noctx
    - staticcheck
formatters:
  enable:
    - gofmt
    - goimports
run:
  timeout: 5m
